# Audio assets

This directory intentionally ships no recorded engine, crowd, commentary, or broadcast audio.

The race presentation uses synthesized Web Audio effects at runtime. Any future recorded file must
be added to `src/assets/credits.json` with its source archive, license, and modification history
before it can be shipped.
